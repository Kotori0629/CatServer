--- a/net/minecraft/block/TripWireHookBlock.java
+++ b/net/minecraft/block/TripWireHookBlock.java
@@ -24,6 +_,7 @@
 import net.minecraft.world.IWorldReader;
 import net.minecraft.world.World;
 import net.minecraft.world.server.ServerWorld;
+import org.bukkit.event.block.BlockRedstoneEvent;
 
 public class TripWireHookBlock extends Block {
    public static final DirectionProperty FACING = HorizontalBlock.FACING;
@@ -137,6 +_,15 @@
          this.playSound(p_176260_1_, blockpos1, flag2, flag3, flag, flag1);
       }
 
+      // CraftBukkit start
+      org.bukkit.block.Block block = p_176260_1_.getWorld().getBlockAt(p_176260_2_.getX(), p_176260_2_.getY(), p_176260_2_.getZ());
+      BlockRedstoneEvent eventRedstone = new BlockRedstoneEvent(block, 15, 0);
+      p_176260_1_.getCBServer().getPluginManager().callEvent(eventRedstone);
+      if (eventRedstone.getNewCurrent() > 0) {
+         return;
+      }
+      // CraftBukkit end
+
       this.playSound(p_176260_1_, p_176260_2_, flag2, flag3, flag, flag1);
       if (!p_176260_4_) {
          p_176260_1_.setBlock(p_176260_2_, blockstate1.setValue(FACING, direction), 3);
@@ -150,8 +_,8 @@
             BlockPos blockpos2 = p_176260_2_.relative(direction, k);
             BlockState blockstate2 = ablockstate[k];
             if (blockstate2 != null) {
-               p_176260_1_.setBlock(blockpos2, blockstate2.setValue(ATTACHED, Boolean.valueOf(flag2)), 3);
-               if (!p_176260_1_.getBlockState(blockpos2).isAir()) {
+               if (!p_176260_1_.getBlockState(blockpos2).isAir()) { // FORGE: fix MC-129055
+                  p_176260_1_.setBlock(blockpos2, blockstate2.setValue(ATTACHED, Boolean.valueOf(flag2)), 3);
                }
             }
          }
