--- a/net/minecraft/block/trees/Tree.java
+++ b/net/minecraft/block/trees/Tree.java
@@ -4,13 +_,16 @@
 import javax.annotation.Nullable;
 import net.minecraft.block.BlockState;
 import net.minecraft.block.Blocks;
+import net.minecraft.block.SaplingBlock;
 import net.minecraft.tags.BlockTags;
 import net.minecraft.util.math.BlockPos;
 import net.minecraft.world.IWorld;
 import net.minecraft.world.gen.ChunkGenerator;
 import net.minecraft.world.gen.feature.BaseTreeFeatureConfig;
 import net.minecraft.world.gen.feature.ConfiguredFeature;
+import net.minecraft.world.gen.feature.Features;
 import net.minecraft.world.server.ServerWorld;
+import org.bukkit.TreeType;
 
 public abstract class Tree {
    @Nullable
@@ -21,6 +_,7 @@
       if (configuredfeature == null) {
          return false;
       } else {
+         setTreeType(configuredfeature); // CraftBukkit
          p_230339_1_.setBlock(p_230339_3_, Blocks.AIR.defaultBlockState(), 4);
          configuredfeature.config.setFromSapling();
          if (configuredfeature.place(p_230339_1_, p_230339_2_, p_230339_5_, p_230339_3_)) {
@@ -41,4 +_,47 @@
 
       return false;
    }
+
+   // CraftBukkit start
+   protected void setTreeType(final ConfiguredFeature<?, ?> worldgentreeabstract) {
+      if (worldgentreeabstract == Features.OAK || worldgentreeabstract == Features.OAK_BEES_005) {
+         SaplingBlock.treeType = TreeType.TREE;
+      } else if (worldgentreeabstract == Features.HUGE_RED_MUSHROOM) {
+         SaplingBlock.treeType = TreeType.RED_MUSHROOM;
+      } else if (worldgentreeabstract == Features.HUGE_BROWN_MUSHROOM) {
+         SaplingBlock.treeType = TreeType.BROWN_MUSHROOM;
+      } else if (worldgentreeabstract == Features.JUNGLE_TREE) {
+         SaplingBlock.treeType = TreeType.COCOA_TREE;
+      } else if (worldgentreeabstract == Features.JUNGLE_TREE_NO_VINE) {
+         SaplingBlock.treeType = TreeType.SMALL_JUNGLE;
+      } else if (worldgentreeabstract == Features.PINE) {
+         SaplingBlock.treeType = TreeType.TALL_REDWOOD;
+      } else if (worldgentreeabstract == Features.SPRUCE) {
+         SaplingBlock.treeType = TreeType.REDWOOD;
+      } else if (worldgentreeabstract == Features.ACACIA) {
+         SaplingBlock.treeType = TreeType.ACACIA;
+      } else if (worldgentreeabstract == Features.BIRCH || worldgentreeabstract == Features.BIRCH_BEES_005) {
+         SaplingBlock.treeType = TreeType.BIRCH;
+      } else if (worldgentreeabstract == Features.OAK_BEES_0002) {
+         SaplingBlock.treeType = TreeType.TALL_BIRCH;
+      } else if (worldgentreeabstract == Features.SWAMP_TREE) {
+         SaplingBlock.treeType = TreeType.SWAMP;
+      } else if (worldgentreeabstract == Features.FANCY_OAK || worldgentreeabstract == Features.FANCY_OAK_BEES_005) {
+         SaplingBlock.treeType = TreeType.BIG_TREE;
+      } else if (worldgentreeabstract == Features.JUNGLE_BUSH) {
+         SaplingBlock.treeType = TreeType.JUNGLE_BUSH;
+      } else if (worldgentreeabstract == Features.DARK_OAK) {
+         SaplingBlock.treeType = TreeType.DARK_OAK;
+      } else if (worldgentreeabstract == Features.MEGA_SPRUCE) {
+         SaplingBlock.treeType = TreeType.MEGA_REDWOOD;
+      } else if (worldgentreeabstract == Features.MEGA_PINE) {
+         SaplingBlock.treeType = TreeType.MEGA_REDWOOD;
+      } else if (worldgentreeabstract != Features.MEGA_JUNGLE_TREE) {
+         SaplingBlock.treeType = TreeType.JUNGLE;
+      } else {
+         SaplingBlock.treeType = TreeType.FORGE_MODS;
+      }
+
+   }
+   // CraftBukkit start
 }
