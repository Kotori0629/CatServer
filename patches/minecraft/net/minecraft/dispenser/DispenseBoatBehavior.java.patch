--- a/net/minecraft/dispenser/DispenseBoatBehavior.java
+++ b/net/minecraft/dispenser/DispenseBoatBehavior.java
@@ -7,6 +_,9 @@
 import net.minecraft.util.Direction;
 import net.minecraft.util.math.BlockPos;
 import net.minecraft.world.World;
+import org.bukkit.craftbukkit.v1_16_R3.inventory.CraftItemStack;
+import org.bukkit.event.block.BlockDispenseEvent;
+import org.bukkit.util.Vector;
 
 public class DispenseBoatBehavior extends DefaultDispenseItemBehavior {
    private final DefaultDispenseItemBehavior defaultDispenseItemBehavior = new DefaultDispenseItemBehavior();
@@ -33,12 +_,34 @@
 
          d3 = 0.0D;
       }
+      // CraftBukkit start
+      ItemStack itemstack2 = p_82487_2_.split(1);
+      org.bukkit.block.Block block = world.getWorld().getBlockAt(p_82487_1_.getPos().getX(), p_82487_1_.getPos().getY(), p_82487_1_.getPos().getZ());
+      CraftItemStack craftItem = CraftItemStack.asCraftMirror(itemstack2);
+      BlockDispenseEvent event = new BlockDispenseEvent(block, craftItem.clone(), new Vector(d0, d1 + d3, d2));
+      if (!DispenserBlock.eventFired) {
+         world.getCBServer().getPluginManager().callEvent(event);
+      }
+      if (event.isCancelled()) {
+         p_82487_2_.grow(1);
+         return p_82487_2_;
+      }
+      if (!event.getItem().equals(craftItem)) {
+         p_82487_2_.grow(1);
+         // Chain to handler for new item
+         final ItemStack eventStack = CraftItemStack.asNMSCopy(event.getItem());
+         final IDispenseItemBehavior idispensebehavior = DispenserBlock.DISPENSER_REGISTRY.get(eventStack.getItem());
+         if (idispensebehavior != IDispenseItemBehavior.NOOP && idispensebehavior != this) {
+            idispensebehavior.dispense(p_82487_1_, eventStack);
+            return p_82487_2_;
+         }
+      }
+      BoatEntity boatentity = new BoatEntity(world, event.getVelocity().getX(), event.getVelocity().getY(), event.getVelocity().getZ());
+      // CraftBukkit end
 
-      BoatEntity boatentity = new BoatEntity(world, d0, d1 + d3, d2);
       boatentity.setType(this.type);
       boatentity.yRot = direction.toYRot();
-      world.addFreshEntity(boatentity);
-      p_82487_2_.shrink(1);
+      if (!world.addFreshEntity(boatentity)) p_82487_2_.shrink(1); // CraftBukkit
       return p_82487_2_;
    }
 
