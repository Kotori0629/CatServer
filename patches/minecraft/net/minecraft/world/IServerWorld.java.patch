--- a/net/minecraft/world/IServerWorld.java
+++ b/net/minecraft/world/IServerWorld.java
@@ -6,7 +_,18 @@
 public interface IServerWorld extends IWorld {
    ServerWorld getLevel();
 
+   default ServerWorld getMinecraftWorld() {
+      return this.getLevel(); // CatServer - remap to real method
+   }
+
    default void addFreshEntityWithPassengers(Entity p_242417_1_) {
-      p_242417_1_.getSelfAndPassengers().forEach(this::addFreshEntity);
-   }
+      if (this.getLevel() != null && this.getLevel().dummyFreshEntitySpawn) return; // Fix Pehkui Startup
+      this.addAllEntities(p_242417_1_, org.bukkit.event.entity.CreatureSpawnEvent.SpawnReason.DEFAULT);
+      // CraftBukkit start
+   }
+
+   default void addAllEntities(Entity entity, org.bukkit.event.entity.CreatureSpawnEvent.SpawnReason reason) {
+      entity.getSelfAndPassengers().forEach((e) -> this.addEntity(e, reason));
+   }
+   // CraftBukkit end
 }
