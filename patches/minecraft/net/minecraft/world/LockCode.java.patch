--- a/net/minecraft/world/LockCode.java
+++ b/net/minecraft/world/LockCode.java
@@ -3,18 +_,32 @@
 import javax.annotation.concurrent.Immutable;
 import net.minecraft.item.ItemStack;
 import net.minecraft.nbt.CompoundNBT;
+import org.bukkit.ChatColor;
+import org.bukkit.craftbukkit.v1_16_R3.util.CraftChatMessage;
 
 @Immutable
 public class LockCode {
    public static final LockCode NO_LOCK = new LockCode("");
-   private final String key;
+   public final String key;
 
    public LockCode(String p_i45903_1_) {
       this.key = p_i45903_1_;
    }
 
    public boolean unlocksWith(ItemStack p_219964_1_) {
-      return this.key.isEmpty() || !p_219964_1_.isEmpty() && p_219964_1_.hasCustomHoverName() && this.key.equals(p_219964_1_.getHoverName().getString());
+      // CraftBukkit start - SPIGOT-6307: Check for color codes if the lock contains color codes
+      if (this.key.isEmpty()) return true;
+      if (!p_219964_1_.isEmpty() && p_219964_1_.hasCustomHoverName()) {
+         if (this.key.indexOf(ChatColor.COLOR_CHAR) == -1) {
+            // The lock key contains no color codes, so let's ignore colors in the item display name (vanilla Minecraft behavior):
+            return this.key.equals(p_219964_1_.getHoverName().getString());
+         } else {
+            // The lock key contains color codes, so let's take them into account:
+            return this.key.equals(CraftChatMessage.fromComponent(p_219964_1_.getHoverName()));
+         }
+      }
+      return false;
+      // CraftBukkit end
    }
 
    public void addToTag(CompoundNBT p_180157_1_) {
